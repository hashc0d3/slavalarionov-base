# Система базовых изображений ремешков по цвету корпуса

## Обзор

Реализована система автоматического отображения ремешков в соответствии с выбранным цветом корпуса часов. Когда пользователь выбирает цвет корпуса на первом шаге конфигуратора, система автоматически подбирает соответствующие изображения ремешков на втором шаге.

## Как это работает

### Шаг 1: Пользователь выбирает цвет корпуса
На первом шаге конфигуратора пользователь выбирает:
- Модель часов
- Размер
- **Цвет корпуса** (например, "Серебристый")

### Шаг 2: Отображение ремешков
На втором шаге система:
1. Получает `colorId` выбранного цвета корпуса
2. Для каждого ремешка ищет в `base_images` изображение с таким же `colorId`
3. Если найдено - отображает это изображение
4. Если не найдено - отображает стандартное изображение ремешка

## Настройка в админ-панели

### 1. Создание цвета

1. Перейти в админ-панель → вкладка **"Цвета"** 🎨
2. Нажать **"Добавить цвет"**
3. Заполнить:
   - **Техническое название**: `silver` (используется в коде и URL изображений)
   - **Наименование для клиента**: `Серебристый` (видит пользователь)
   - **HEX цвет**: `#C0C0C0` (для визуального отображения)
4. Сохранить

### 2. Добавление цвета к модели часов

1. Перейти в **"Модели часов"**
2. Редактировать нужную модель
3. В поле **"Доступные цвета корпуса"** выбрать нужные цвета из выпадающего списка
   - Можно выбрать несколько цветов сразу (например, "Серебристый", "Чёрный", "Золотой")
   - Цвета отображаются с цветными кружками для визуального различия
   - Используйте поиск для быстрого нахождения нужного цвета
4. Сохранить модель

### 3. Добавление базовых изображений к ремешку

1. Перейти в **"Ремешки"**
2. Редактировать нужный ремешок
3. Перейти на вкладку **"Базовые изображения"**
4. Нажать **"Добавить цвет"**
5. Выбрать цвет из списка (например, "Серебристый")
6. Загрузить изображения ремешка в этом цвете:
   - **Вид 1**: основное изображение
   - **Вид 2**: второй ракурс (опционально)
   - **Вид 3**: третий ракурс (опционально)
7. Сохранить

> **Важно:** Для каждого ремешка можно добавить несколько цветов. Например, если ремешок доступен в серебристом, чёрном и золотом вариантах, добавьте все три цвета с соответствующими изображениями.

## Примеры использования

### Пример 1: Apple Watch с серебристым корпусом

**Шаг 1:**
- Пользователь выбирает "Apple Watch 7-9 серия"
- Размер: 45mm
- Цвет корпуса: **Серебристый** (colorId = 1)

**Шаг 2:**
Система отображает все доступные ремешки:
- Ремешок "Brogue" показывается с изображением для серебристого цвета (если добавлено)
- Ремешок "Classic" показывается с изображением для серебристого цвета (если добавлено)
- Если для ремешка нет изображения в серебристом цвете - показывается стандартное изображение

### Пример 2: Apple Watch с чёрным корпусом

**Шаг 1:**
- Пользователь выбирает "Apple Watch Ultra"
- Размер: 49mm
- Цвет корпуса: **Чёрный** (colorId = 2)

**Шаг 2:**
Все ремешки отображаются с изображениями для чёрного цвета (если добавлены)

## Технические детали

### Store (configuratorStore)

#### Новые геттеры:
- `selectedFrameColorId` - возвращает ID выбранного цвета корпуса
- `getStrapBaseImageForFrameColor(strap)` - возвращает базовое изображение ремешка для выбранного цвета

#### Пример использования:
```typescript
const frameColorId = configuratorStore.selectedFrameColorId // 1 (silver)
const baseImage = configuratorStore.getStrapBaseImageForFrameColor(strap)
// Возвращает: "/uploads/strap-colors/brogue/view1/image-silver.png"
```

### Компоненты отображения ремешков

#### StrapModelStep (Шаг 2 - выбор ремешка)

Обновлён метод `getStrapImage(strap)`:
1. Пытается получить базовое изображение для выбранного цвета корпуса
2. Если не найдено - возвращает стандартное изображение ремешка

```typescript
const getStrapImage = (strap: any): string => {
  const baseImage = configuratorStore.getStrapBaseImageForFrameColor(strap)
  if (baseImage) return baseImage
  
  // Fallback на стандартное изображение
  return strap.attributes.watch_strap.preview_image || '/window.svg'
}
```

#### StrapDesignPreview (Шаг 3 - превью дизайна)

Обновлена функция `getBaseViewImage(view)` с приоритетной логикой:

1. **Приоритет 1:** Базовое изображение для выбранного цвета корпуса
   - Ищет в `base_images` изображение с `colorId` равным выбранному цвету
   - Если найдено - использует его для соответствующего вида (view1/view2/view3)

2. **Приоритет 2:** Изображения из `strap_params.view_images`
   - Если базовое изображение по цвету не найдено
   - Использует общие изображения из параметров ремешка

3. **Приоритет 3:** Legacy хардкодные URL
   - Если ничего не найдено выше
   - Использует стандартные URL с хешами

### API Response

Backend возвращает ремешки с полем `base_images`:

```json
{
  "id": 1,
  "strap_name": "brogue",
  "strap_title": "Brogue",
  "price": 8990,
  "preview_image": "/uploads/brogue_preview.png",
  "base_images": [
    {
      "id": 1,
      "colorId": 1,
      "view1Image": "/uploads/strap-colors/brogue/view1/1234-silver.png",
      "view2Image": "/uploads/strap-colors/brogue/view2/1234-silver.png",
      "view3Image": "/uploads/strap-colors/brogue/view3/1234-silver.png",
      "color": {
        "id": 1,
        "technical_name": "silver",
        "display_name": "Серебристый",
        "hex_code": "#C0C0C0"
      }
    },
    {
      "id": 2,
      "colorId": 2,
      "view1Image": "/uploads/strap-colors/brogue/view1/5678-black.png",
      "color": {
        "id": 2,
        "technical_name": "black",
        "display_name": "Чёрный",
        "hex_code": "#000000"
      }
    }
  ]
}
```

## Рекомендации

### Для админа:
1. **Создайте все основные цвета** перед добавлением моделей и ремешков
2. **Используйте единые технические названия** (например, `silver`, а не `silver_color`)
3. **Загружайте качественные изображения** с одинаковым освещением для всех цветов
4. **Добавляйте базовые изображения постепенно** - начните с самых популярных цветов

### Для фотографа:
1. Делайте фото ремешков на **нейтральном фоне**
2. Используйте **одинаковое освещение** для всех цветов
3. Сохраняйте **одинаковый ракурс** для всех видов
4. Размер изображений: **рекомендуется 800x600px**

## Обратная совместимость

Система полностью обратно совместима:
- Если у ремешка нет базовых изображений - показывается стандартное изображение
- Старые ремешки продолжают работать без изменений
- Базовые изображения можно добавлять постепенно

