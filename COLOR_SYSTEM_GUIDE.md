# Централизованная система управления цветами

## Обзор

Реализована централизованная система управления цветами для моделей часов и ремешков. Теперь цвета хранятся в отдельной таблице, что позволяет:
- Единообразно управлять цветами через админ-панель
- Избежать дублирования цветов
- Легко обновлять HEX-коды и наименования

## Структура базы данных

### Таблица `colors`
Основная таблица цветов:
- `id` - уникальный идентификатор
- `technical_name` - техническое название (например, "silver", "black")
- `display_name` - наименование для клиента (например, "Серебристый", "Чёрный")
- `hex_code` - HEX цвет (например, "#C0C0C0")

### Таблица `frame_colors`
Связывает модели часов с цветами корпуса:
- `id` - уникальный идентификатор
- `colorId` - ссылка на цвет из таблицы `colors`
- `watchModelId` - ссылка на модель часов
- `view1Image`, `view2Image`, `view3Image` - изображения для 3 видов

### Таблица `strap_base_images`
Базовые изображения ремешков для каждого цвета:
- `id` - уникальный идентификатор
- `colorId` - ссылка на цвет из таблицы `colors`
- `watchStrapId` - ссылка на ремешок
- `view1Image`, `view2Image`, `view3Image` - изображения для 3 видов

## Backend API

### Управление цветами
- `GET /api/colors` - получить все цвета
- `GET /api/colors/:id` - получить цвет по ID
- `POST /api/colors` - создать новый цвет
- `PUT /api/colors/:id` - обновить цвет
- `DELETE /api/colors/:id` - удалить цвет

### Базовые изображения ремешков
- `POST /api/watch-straps/:id/base-images` - добавить базовое изображение
- `PUT /api/watch-straps/:strapId/base-images/:imageId` - обновить изображение
- `DELETE /api/watch-straps/:strapId/base-images/:imageId` - удалить изображение

## Админ-панель

### Вкладка "Цвета" 🎨
Новая вкладка для управления цветами:
1. Список всех цветов с превью
2. Добавление нового цвета:
   - Техническое название
   - Наименование для клиента
   - HEX цвет (с визуальным выбором)
3. Редактирование и удаление цветов

### Модели часов
Обновлена логика добавления цветов корпуса:
1. При добавлении цвета теперь выбирается из списка доступных цветов
2. Для каждого цвета можно загрузить 3 вида изображений
3. Изображения автоматически загружаются в `/uploads/strap-colors/frame/viewN/`

### Ремешки
Добавлена новая вкладка "Базовые изображения":
1. Для каждого ремешка можно добавить несколько цветов
2. Для каждого цвета - 3 вида изображений
3. Изображения загружаются в `/uploads/strap-colors/{strap_name}/viewN/`

## Миграция данных

База данных автоматически обновлена миграцией `20251204155531_add_colors_table`:
1. Создана таблица `colors` с базовыми цветами
2. Обновлена таблица `frame_colors` для связи с `colors`
3. Создана таблица `strap_base_images`
4. Существующие данные сохранены

## Использование

### Добавление нового цвета
1. Перейти в админ-панель → вкладка "Цвета"
2. Нажать "Добавить цвет"
3. Заполнить:
   - Техническое название: `rose-gold` (для кода)
   - Наименование для клиента: `Розовое золото` (для пользователя)
   - HEX цвет: `#B8977E`
4. Сохранить

### Добавление цвета корпуса к модели часов
1. Перейти в "Модели часов"
2. Редактировать модель
3. В поле **"Доступные цвета корпуса"** выбрать нужные цвета из выпадающего списка
   - Можно выбрать несколько цветов
   - Цвета отображаются с цветными кружками для удобства
4. Сохранить модель

### Добавление базовых изображений ремешка
1. Перейти в "Ремешки"
2. Редактировать ремешок
3. Перейти на вкладку "Базовые изображения"
4. Нажать "Добавить цвет"
5. Выбрать цвет из списка
6. Загрузить изображения для 3 видов
7. Сохранить

## Технические детали

### Frontend
- `frontend/src/shared/api/colors.api.ts` - API клиент для работы с цветами
- `frontend/src/widgets/admin/ui/AdminColorsMantine.tsx` - компонент управления цветами
- `frontend/src/widgets/admin/ui/StrapBaseImagesEditor.tsx` - редактор базовых изображений
- Обновлены типы в `configurator.store.ts` для поддержки новой структуры

### Backend
- `backend/src/colors/` - модуль управления цветами
- `backend/prisma/schema.prisma` - обновлённая схема БД
- Обновлены сервисы `watch-models` и `watch-straps` для работы с цветами

## Обратная совместимость

Система поддерживает обратную совместимость:
- API возвращает `color_name` и `color_code` для старого кода
- Типы TypeScript включают опциональные поля для совместимости
- Существующие бэкапы можно восстановить (с автоматической конвертацией)

