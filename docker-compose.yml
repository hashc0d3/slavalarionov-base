services:
  backend:
    build:
      context: /Users/user/slava-larionov/site-base
      dockerfile: backend/Dockerfile
    environment:
      NODE_ENV: production
      PORT: 8081
      DATABASE_URL: "file:./prisma/dev.db"
    ports:
      - "8081:8081"
    working_dir: /app/backend

