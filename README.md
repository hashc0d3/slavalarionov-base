# Next.js + NestJS Middleware Monorepo

## Описание

Монорепозиторий с фронтендом на Next.js и бэкендом на NestJS, объединёнными через middleware.  
Фронт и бэкенд разделены по папкам `frontend` и `backend`.

## Структура

- `frontend/` — Next.js приложение (React)
- `backend/` — NestJS сервер (API + middleware для Next.js)

## Запуск

### Быстрый запуск одной командой

```bash
./start.sh
```

Этот скрипт автоматически:
- Проверит наличие Docker и Docker Compose
- Установит зависимости и сгенерирует Prisma client (если нужно)
- Соберет и запустит проект через Docker Compose
- Покажет статус и логи

Приложение будет доступно по адресу: http://localhost:8081

### Локальная разработка

1. Установить зависимости:
   ```bash
   npm install
   cd backend && npm install
   cd ../frontend && npm install
   ```

2. Запустить backend (NestJS + Next.js):
   ```bash
   cd backend
   npm run start:dev
   ```

3. Открыть:
   - [http://localhost:8081](http://localhost:8081) — главная страница
   - [http://localhost:8081/configurator](http://localhost:8081/configurator) — **основной конструктор**
   - [http://localhost:8081/api/greeting](http://localhost:8081/api/greeting) — пример API

### Развертывание на сервере через Docker Compose

1. Клонировать репозиторий:
   ```bash
   git clone <repository-url>
   cd site-base
   ```

2. Установить зависимости и сгенерировать Prisma client:
   ```bash
   cd backend && npm install
   npm run prisma:generate
   cd ../frontend && npm install
   cd ..
   ```
   
   **Важно**: Prisma client должен быть сгенерирован локально перед сборкой Docker образа, чтобы избежать проблем с загрузкой бинарников.

3. Запустить сборку и запуск через Docker Compose:
   ```bash
   docker compose up -d --build
   ```

4. Проверить статус:
   ```bash
   docker compose ps
   docker compose logs -f backend
   ```

5. Открыть приложение:
   - [http://localhost:8081](http://localhost:8081) — главная страница

**Примечание**: Сборка оптимизирована для ускорения:
- Используется SWC компилятор вместо TypeScript (быстрее в 5-20 раз)
- Отключена проверка типов во время сборки
- Кэширование промежуточных результатов
- Отключены ненужные оптимизации
- Если Prisma client сгенерирован локально, он будет использован (не нужно загружать бинарники)

**Если возникают проблемы с Prisma generate**:
- Dockerfile автоматически попытается сгенерировать Prisma client с повторными попытками
- Если сервер Prisma недоступен, сгенерируйте Prisma client локально перед сборкой:
  ```bash
  cd backend && npm run prisma:generate
  ```

## Основные команды

### Backend
- `npm run start:dev` — запуск backend с hot-reload
- `npm run build` — сборка backend
- `npm run start` — запуск backend в production-режиме

### База данных (Prisma)
- `npm run prisma:generate` — генерация Prisma Client
- `npm run prisma:migrate` — создание и применение миграций
- `npm run prisma:seed` — заполнение БД начальными данными (4 модели часов + 5 ремешков)
- `npm run prisma:studio` — открыть Prisma Studio (GUI для БД)

## Прокси API

- Все запросы к `/api/*` обрабатываются NestJS-контроллерами.
- Остальные запросы обслуживаются Next.js.

### API Endpoints:

**Модели часов:**
- `GET /api/watch-models` - получить все модели
- `GET /api/watch-models/:id` - получить модель по ID
- `POST /api/watch-models` - создать модель
- `PUT /api/watch-models/:id` - обновить модель
- `DELETE /api/watch-models/:id` - удалить модель
- `GET /api/watch-models/backup` - создать бэкап
- `POST /api/watch-models/restore` - восстановить из бэкапа

**Ремешки:**
- `GET /api/watch-straps` - получить все ремешки
- `GET /api/watch-straps/:id` - получить ремешок по ID
- `POST /api/watch-straps` - создать ремешок
- `PUT /api/watch-straps/:id` - обновить ремешок
- `DELETE /api/watch-straps/:id` - удалить ремешок
- `GET /api/watch-straps/backup` - создать бэкап
- `POST /api/watch-straps/restore` - восстановить из бэкапа

## Примечания

- Папка `.idea` и артефакты сборки не должны попадать в git (см. `.gitignore`).
- Для разработки фронта и бэкенда используются отдельные `tsconfig.json` и `package.json`.
- Проект использует Next.js 15 и React 19.
- Конфигурация Next.js в формате `.ts`.
- **Админ-панель использует Mantine UI** - современную библиотеку компонентов для React.

## Админ-панель

Для доступа к админ-панели управления моделями часов:

1. Откройте консоль браузера (F12)
2. Выполните команду:
   ```javascript
   localStorage.setItem('ROLE', 'ADMIN')
   ```
3. Обновите страницу
4. Появится кнопка "🔧 Админ-панель" в правом верхнем углу
5. Перейдите по ссылке `/admin` или нажмите на кнопку

### Возможности админ-панели:

#### 📱 Модели часов (первый шаг)
- ✅ Добавление новых моделей часов
- ✅ Редактирование существующих моделей (название, производитель, картинка)
- ✅ Удаление моделей
- ✅ Управление размерами моделей (добавление/удаление через Pills)
- ✅ Управление цветами корпуса (добавление/удаление через Pills)
- ✅ **Управление доступными ремешками** - удобный MultiSelect с поиском (NEW!)
- ✅ **Загрузка изображений** с устройства или по URL
- ✅ **Создание бэкапа** - скачивание всех моделей в JSON
- ✅ **Восстановление из бэкапа** - загрузка JSON файла для восстановления
- ✅ Автосохранение в базу данных (SQLite)
- ✅ Сброс к начальным данным

#### ⌚ Ремешки (второй шаг)
- ✅ Добавление новых моделей ремешков
- ✅ Редактирование существующих ремешков (5 начальных: Butterfly, Classic, Double Wrap, Brogue, Minimal)
- ✅ Удаление ремешков
- ✅ Управление цветами кожи (добавление/удаление, Brogue имеет 19 цветов!)
- ✅ Управление цветами строчки (добавление/удаление)
- ✅ Управление цветами края (добавление/удаление)
- ✅ Управление цветами пряжки (добавление/удаление)
- ✅ Управление цветами адаптера (добавление/удаление)
- ✅ Настройка наличия пряжки-бабочки
- ✅ **Создание бэкапа ремешков** - скачивание в JSON
- ✅ **Восстановление ремешков из бэкапа**
- ✅ Автосохранение в базу данных (SQLite)
- ✅ **Все данные с сайта перенесены в админку** - данные загружаются из БД при старте конфигуратора

**Админка разделена на вкладки** для удобного управления моделями часов и ремешками отдельно.

**NEW!** Админка на Mantine UI:
- 🎨 **Современный дизайн** - использует Mantine UI v7
- 📋 **Модальные окна** - редактирование в удобных модалках
- 🔔 **Уведомления** - toast-сообщения о результате операций (успех/ошибка)
- 🎯 **MultiSelect для ремешков** - выбор доступных ремешков с поиском и фильтрацией
- 🏷️ **Pills и Badges** - компактное отображение размеров и цветов
- 📱 **Responsive дизайн** - адаптивная верстка
- 📁 **Загрузка изображений** - удобные кнопки для выбора файла с устройства или ввода URL
- 🖼️ **Превью изображений** - показывает загруженное изображение с возможностью удаления
- ⚡ **Кнопки быстрого действия** - для работы с изображениями и мультиселектором
- 🎨 **Управление параметрами дизайна** - добавление/редактирование/удаление цветов для всех типов параметров
- 🎯 **Цветовые пикеры** - удобный выбор цветов с предпросмотром
- 💰 **Дополнительные цены** - возможность установить дополнительную стоимость для каждого цвета
- 💾 **Полный backup** - создание и восстановление всех данных админки одним файлом
- По умолчанию все ремешки доступны для всех моделей

### Загрузка изображений:
При редактировании модели или ремешка:
1. Нажмите кнопку **"📁 Выбрать изображение с устройства"**
2. Или введите URL в поле **"или введите URL изображения"**
3. Превью появится автоматически
4. Кнопка **"🗑️ Удалить"** удаляет изображение

### Фильтрация ремешков по модели:
Конфигуратор **автоматически фильтрует** доступные ремешки:
- На шаге 2 (выбор ремешка) показываются только те ремешки, которые вы указали как доступные для выбранной модели часов в админке
- Если для модели не указаны доступные ремешки, показываются все
- Это позволяет создавать разные наборы ремешков для разных моделей часов (например, Ultra может иметь свой набор)

### Синхронизация селекторов:
- Селекторы в верхней части конфигуратора **автоматически обновляются** при выборе модели часов
- При смене модели часов обновляется информация в `steps.model.modelName` и `steps.model.modelSize`
- Это обеспечивает корректное отображение выбранной модели в селекторах

### Исправление отображения модели в селекторе:
- **Проблема**: Селектор показывал неправильную информацию о выбранной модели (например, "Apple Watch" вместо "10 серия")
- **Решение**: Изменен `value` селектора с `configuratorStore.steps.model.modelName` на `configuratorStore.selectedWatchModel?.watch_model_name`
- **Результат**: Селектор теперь корректно отображает `watch_model_name` (например, "10 серия") вместо `model_name` (например, "Apple Watch")

### Управление параметрами дизайна ремешков:
- **Новая функциональность**: Добавлена возможность управлять всеми параметрами дизайна ремешков в админке
- **Вкладки**: Модальное окно редактирования ремешка разделено на "Основные параметры" и "Параметры дизайна"
- **Типы параметров**: 
  - 🟦 **Цвета кожи** - основные цвета ремешка
  - 🟩 **Цвета строчки** - цвета швов
  - 🟧 **Цвета края** - цвета обработки краев
  - 🟪 **Цвета пряжки** - цвета застежки
  - 🟦 **Цвета адаптера** - цвета крепления к часам
- **Функции**: Добавление, редактирование, удаление цветов с простым интерфейсом
- **Интерфейс**: 
  - 🎨 **Круглые цветовые индикаторы** - цвета отображаются как кружки
  - 🔘 **Кнопки действий** - маленькие кнопки "Редактировать" и "Удалить"
  - 👁️ **Предпросмотр** - в модальном окне показывается предпросмотр цвета
  - 🎯 **Простой интерфейс** - без дополнительных цен и сложных элементов

### Бэкап и восстановление:

**Полный бэкап всех данных:**
1. **Скачать полный бэкап**: Нажмите "💾 Скачать полный бэкап" в верхней части админки - создастся файл `admin-full-backup-YYYY-MM-DD.json` со всеми данными
2. **Восстановить из полного бэкапа**: Нажмите "📁 Восстановить из бэкапа" и выберите файл полного бэкапа
3. Все данные (модели часов + ремешки) будут восстановлены из одного файла

**Отдельные бэкапы:**

**Для моделей часов:**
1. **Скачать бэкап**: Нажмите "💾 Скачать бэкап" в разделе "Модели часов" - создастся файл `watch-models-backup-YYYY-MM-DD.json`
2. **Загрузить бэкап**: Нажмите "📂 Загрузить бэкап" - выберите ранее скачанный JSON файл
3. Все модели часов из файла будут восстановлены в базе данных

**Для ремешков:**
1. **Скачать бэкап**: Нажмите "💾 Скачать бэкап" в разделе "Ремешки" - создастся файл `watch-straps-backup-YYYY-MM-DD.json`
2. **Загрузить бэкап**: Нажмите "📂 Загрузить бэкап" - выберите ранее скачанный JSON файл  
3. Все ремешки из файла будут восстановлены в базе данных

**Примечание**: 
- **Полный бэкап** содержит все данные админки в одном файле
- **Отдельные бэкапы** позволяют восстанавливать только модели часов или только ремешки
- Все бэкапы восстанавливаются независимо друг от друга
- **Восстановление связей** - при восстановлении автоматически восстанавливаются связи между моделями и ремешками
- **Проверка файлов** - система проверяет формат, размер и структуру файлов бэкапа

## Решение проблем

### Ошибка "Unique constraint failed on watch_model_name"

Если при сбросе к начальным данным возникает ошибка:
```
PrismaClientKnownRequestError: Unique constraint failed on the fields: (`watch_model_name`)
```

**Причина**: Попытка создать модель часов с именем, которое уже существует в базе данных.

**Решение**: Функция `resetWatchModelsToDefault()` теперь использует endpoint `/api/watch-models/restore`, который:
1. Удаляет все существующие модели из БД
2. Создаёт новые модели из начальных данных
3. Загружает обновлённые данные с сервера

Если проблема сохраняется, можно вручную очистить базу данных:
```bash
cd backend
rm prisma/dev.db
npm run prisma:migrate
npm run prisma:seed
```

### Ошибка "Hydration failed" в Next.js

Если в консоли браузера появляется ошибка:
```
Error: Hydration failed because the initial UI does not match what was rendered on the server.
```

**Причина**: Несоответствие между серверным и клиентским рендерингом из-за обращения к `localStorage` во время инициализации store.

**Решение**: Store теперь инициализируется с `mockWatchModels` как на сервере, так и на клиенте. Данные из API загружаются через `useEffect` после монтирования компонента, что предотвращает несоответствие при гидратации.

Если ошибка сохраняется:
1. Очистите localStorage: откройте консоль браузера и выполните `localStorage.clear()`
2. Перезагрузите страницу
3. Проверьте, что файлы пересобрались после изменений

### Ошибка "PayloadTooLargeError: request entity too large"

Если при загрузке бэкапа или сбросе к начальным данным возникает ошибка:
```
PayloadTooLargeError: request entity too large
expected: 2486081,
length: 2486081,
limit: 102400
```

**Причина**: Размер данных превышает лимит тела запроса (по умолчанию 100KB). Это происходит при отправке моделей с base64-кодированными изображениями.

**Решение**: Лимит размера тела запроса увеличен до 10MB в `/backend/src/main.ts`. После перезапуска сервера ошибка должна исчезнуть.

Если проблема сохраняется, убедитесь что:
1. Сервер был перезапущен после изменений
2. В `main_image` не используются слишком большие изображения (рекомендуется до 2MB на изображение)

### Ошибка "Model ID not found" при удалении или редактировании

Если при попытке удалить или отредактировать модель часов появляется сообщение:
```
Model ID not found
```

**Причина**: У моделей отсутствует поле `id`, которое необходимо для операций обновления и удаления. Это может произойти если:
- Данные не были загружены с сервера
- Произошла ошибка при загрузке данных из API

**Решение**: 
1. Функция `mapDBToStore` теперь правильно включает поле `id` при преобразовании данных из БД
2. При отсутствии ID показывается понятное сообщение с просьбой перезагрузить страницу
3. Перезагрузите страницу - данные должны загрузиться с сервера с корректными ID

Если проблема сохраняется:
1. Откройте консоль браузера (F12) и проверьте наличие ошибок при загрузке данных
2. Убедитесь, что backend запущен и доступен по адресу `/api/watch-models`
3. Проверьте, что в базе данных есть модели с корректными ID
